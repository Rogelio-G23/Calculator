# Calculator
The multiplication, addition, and subtraction operators in calculator.

The code was made with method function to calculate the given numbers and imports the scanner to collect the data inputs.
